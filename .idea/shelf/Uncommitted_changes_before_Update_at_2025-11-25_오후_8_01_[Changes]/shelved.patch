Index: parse.py
===================================================================
diff --git a/parse.py b/parse.py
deleted file mode 100644
--- a/parse.py	(revision 980164c90b0c6a6a4f1ec393672b2b961a552161)
+++ /dev/null	(revision 980164c90b0c6a6a4f1ec393672b2b961a552161)
@@ -1,148 +0,0 @@
-import pandas as pd
-import sys
-import os
-from datetime import datetime
-
-# 출력을 파일로도 저장 -> ????
-output_log = open('parse_output.txt', 'w', encoding='utf-8')
-
-def log_print(*args, **kwargs):
-    """print와 동시에 파일에도 기록"""
-    message = ' '.join(str(arg) for arg in args)
-    print(message, **kwargs)
-    output_log.write(message + '\n')
-    output_log.flush()
-
-# Parquet 파일 읽기
-parquet_file = 'data/가계부_데이터_저장용.parquet'
-log_print(f"Parquet 파일 읽는 중: {parquet_file}")
-
-try:
-    df = pd.read_parquet(parquet_file)
-    log_print(f"원본 데이터 행 수: {len(df)}")
-    log_print(f"컬럼: {df.columns.tolist()}")
-except Exception as e:
-    log_print(f"오류 발생: {str(e)}")
-    output_log.close()
-    sys.exit(1)
-
-# 컬럼명 찾기
-date_cols = [col for col in df.columns if any(keyword in str(col).lower() for keyword in ['날짜', 'date', '일시'])]
-time_cols = [col for col in df.columns if any(keyword in str(col).lower() for keyword in ['시간', 'time'])]
-amount_cols = [col for col in df.columns if any(keyword in str(col).lower() for keyword in ['금액', 'amount', '가격', 'price', '지출', '출금'])]
-main_category_cols = [col for col in df.columns if any(keyword in str(col).lower() for keyword in ['대분류', 'main', 'category', '카테고리'])]
-
-# 필수 컬럼 확인
-if not date_cols:
-    log_print("경고: 날짜 컬럼을 찾을 수 없습니다. 첫 번째 컬럼을 사용합니다.")
-    date_cols = [df.columns[0]]
-if not amount_cols:
-    log_print("경고: 금액 컬럼을 찾을 수 없습니다. 숫자형 컬럼을 찾습니다.")
-    numeric_cols = df.select_dtypes(include=[float, int]).columns.tolist()
-    if numeric_cols:
-        amount_cols = [numeric_cols[0]]
-    else:
-        log_print("오류: 금액 컬럼을 찾을 수 없습니다.")
-        output_log.close()
-        sys.exit(1)
-if not main_category_cols:
-    log_print("오류: 대분류 컬럼을 찾을 수 없습니다.")
-    output_log.close()
-    sys.exit(1)
-
-date_col = date_cols[0]
-amount_col = amount_cols[0]
-main_category_col = main_category_cols[0]
-time_col = time_cols[0] if time_cols else None
-
-log_print(f"\n사용할 컬럼:")
-log_print(f"  날짜: {date_col}")
-if time_col:
-    log_print(f"  시간: {time_col}")
-log_print(f"  금액: {amount_col}")
-log_print(f"  대분류: {main_category_col}")
-
-# 날짜 컬럼이 datetime이 아니면 변환
-if not pd.api.types.is_datetime64_any_dtype(df[date_col]):
-    df[date_col] = pd.to_datetime(df[date_col], errors='coerce')
-
-# 시간(hour) 추출
-if time_col:
-    # 시간 컬럼이 있으면 시간 컬럼에서 추출 (뒤 6자리 패딩 제거)
-    if pd.api.types.is_datetime64_any_dtype(df[time_col]):
-        # datetime 형태면 직접 시간 추출
-        df['hour'] = df[time_col].dt.hour
-    else:
-        # 숫자나 문자열 형태면 뒤 6자리 패딩 제거 후 시간 추출
-        # 시간 컬럼을 문자열로 변환
-        time_str = df[time_col].astype(str)
-        # 뒤 6자리 제거 (패딩 제거) - 길이가 6자리보다 긴 경우만
-        time_str = time_str.apply(lambda x: x[:-6] if len(x) > 6 else x)
-        # 시간 추출 (예: "12000000" -> "12" -> 12)
-        df['hour'] = pd.to_numeric(time_str, errors='coerce').astype('Int64')
-else:
-    # 시간 컬럼이 없으면 날짜 컬럼에서 시간 추출
-    df['hour'] = df[date_col].dt.hour
-
-# 금액 컬럼이 숫자가 아니면 변환
-if df[amount_col].dtype == 'object':
-    df[amount_col] = df[amount_col].astype(str).str.replace(',', '').str.replace('원', '').str.replace(' ', '').str.replace('-', '')
-df[amount_col] = pd.to_numeric(df[amount_col], errors='coerce')
-df[amount_col] = df[amount_col].abs()  # 음수인 경우 절댓값으로 변환
-
-# 필터링 조건
-# 1. 대분류가 "식사"인 것
-식사_조건 = df[main_category_col].astype(str).str.contains('식사', na=False, case=False, regex=True)
-
-# 2. 한건에 5만원 이상 또는 오후 6시에서 오전 6시 사이
-금액_조건 = df[amount_col] >= 50000
-시간_조건 = (df['hour'] >= 18) | (df['hour'] < 6)
-
-# 최종 조건: 대분류가 식사 AND (금액 >= 50000 OR 시간 조건)
-최종_조건 = 식사_조건 & (금액_조건 | 시간_조건)
-
-# 필터링된 데이터
-result_df = df[최종_조건].copy()
-
-log_print(f"\n=== 필터링 결과 ===")
-log_print(f"조건에 맞는 데이터: {len(result_df)}건")
-
-if len(result_df) > 0:
-    log_print(f"\n필터링된 데이터:")
-    log_print(result_df[[date_col, main_category_col, amount_col, 'hour']].to_string())
-    
-    # parse 폴더 생성
-    os.makedirs('parse', exist_ok=True)
-    
-    # Parquet로 저장
-    output_file = 'parse/식사_필터링_결과.parquet'
-    try:
-        result_df.to_parquet(output_file, index=False, engine='pyarrow')
-        log_print(f"\n결과를 '{output_file}'에 저장했습니다.")
-    except Exception as e:
-        log_print(f"Parquet 저장 오류 (pyarrow 미설치 가능): {str(e)}")
-        try:
-            result_df.to_parquet(output_file, index=False, engine='fastparquet')
-            log_print(f"Parquet 파일 저장 완료 (fastparquet 사용): {output_file}")
-        except Exception as e2:
-            log_print(f"Parquet 저장 실패: {str(e2)}")
-            output_log.close()
-            sys.exit(1)
-    
-    # 통계 정보
-    log_print(f"\n=== 통계 정보 ===")
-    금액_충족 = result_df[result_df[amount_col] >= 50000]
-    시간_충족 = result_df[(result_df['hour'] >= 18) | (result_df['hour'] < 6)]
-    모두_충족 = result_df[(result_df[amount_col] >= 50000) & ((result_df['hour'] >= 18) | (result_df['hour'] < 6))]
-    
-    log_print(f"금액 조건 충족 (5만원 이상): {len(금액_충족)}건")
-    log_print(f"시간 조건 충족 (오후 6시~오전 6시): {len(시간_충족)}건")
-    log_print(f"두 조건 모두 충족: {len(모두_충족)}건")
-    log_print(f"총 금액: {result_df[amount_col].sum():,.0f}원")
-    log_print(f"평균 금액: {result_df[amount_col].mean():,.0f}원")
-else:
-    log_print("조건에 맞는 데이터가 없습니다.")
-
-# 로그 파일 닫기
-output_log.close()
-
Index: test1.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/test1.py b/test1.py
new file mode 100644
--- /dev/null	(date 1764068377297)
+++ b/test1.py	(date 1764068377297)
@@ -0,0 +1,49 @@
+import pandas as pd
+from pathlib import Path
+from datetime import datetime
+
+from logger import setup_logger
+
+logger = setup_logger(__name__)
+
+SOURCE = Path("data/가계부_데이터_저장용.parquet")
+TARGET = Path("data/커피간식.parquet")
+
+def main():
+    df = pd.read_parquet(SOURCE)
+    logger.info("원본 로드: rows=%d, columns=%s", len(df), list(df.columns))
+
+    # 날짜는 그대로 datetime, 시간은 timedelta로 변환
+    date_part = pd.to_datetime(df["날짜"])
+    time_part = df["시간"].apply(
+        lambda t: pd.to_timedelta(
+            datetime.combine(datetime.min, t) - datetime.min
+        )
+        if pd.notna(t) else pd.NaT
+    )
+    df["거래일"] = date_part + time_part
+    df = df.dropna(subset=["거래일"])
+    df["week_id"] = df["거래일"].dt.to_period("W").apply(lambda p: p.start_time)
+    logger.info("거래일 계산 후: rows=%d", len(df))
+
+    # 1) 커피/간식만
+    filtered = df[df["대분류"].isin(["카페/간식"])].copy()
+    logger.info("대분류 필터 후: rows=%d", len(filtered))
+    logger.info("대분류 분포: %s", filtered["대분류"].value_counts().to_dict())
+
+    # 2) 주별 거래 횟수 계산
+    weekly_counts = filtered.groupby("week_id")["week_id"].transform("count")
+    logger.info("주당 최대 거래 수: %s", weekly_counts.max())
+
+    # 3) 건당 2만 이상이거나, 같은 주에 5회 이상 나온 거래만 남기기
+    result = filtered[
+        (filtered["금액"].abs() >= 5000) | (weekly_counts >= 5)
+    ].reset_index(drop=True)
+    logger.info("최종 필터 후: rows=%d", len(result))
+    logger.info("금액 분포: %s", result["금액"].describe().to_dict())
+
+    result.to_parquet(TARGET, index=False)
+    logger.info(f"{len(result)}건 저장 완료 → {TARGET}") 
+
+if __name__ == "__main__":
+    main()
\ No newline at end of file
